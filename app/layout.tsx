import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SoftWebix - Web Design, Mobile Apps & IT Solutions',
  description: 'Hong Kong\'s First One-Stop Website Design, Mobile App Development & IT Solutions Company. We make it simple and affordable.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
